// import { EntityOptions } from 'typeorm';

declare module 'typeorm/decorator/entity/Entity' {
  // type InferableClass = (clz: C) => C;
  // // node_modules/typeorm/decorator/entity/Entity.d.ts
  // export interface EntityInterface {
  //   /**
  //    * This decorator is used to mark classes that will be an entity (table or document depend on database type).
  //    * Database schema will be created for all classes decorated with it, and Repository can be retrieved and used for it.
  //    */
  //   (options?: EntityOptions): InferableClass;
  //   /**
  //    * This decorator is used to mark classes that will be an entity (table or document depend on database type).
  //    * Database schema will be created for all classes decorated with it, and Repository can be retrieved and used for it.
  //    */
  //   (name?: string, options?: EntityOptions): InferableClass;
  // }
  // /**
  //  * This decorator is used to mark classes that will be an entity (table or document depend on database type).
  //  * Database schema will be created for all classes decorated with it, and Repository can be retrieved and used for it.
  //  */
  // export declare function Entity(options?: EntityOptions): InferableClass;
  // /**
  //  * This decorator is used to mark classes that will be an entity (table or document depend on database type).
  //  * Database schema will be created for all classes decorated with it, and Repository can be retrieved and used for it.
  //  */
  // export declare function Entity(name?: string, options?: EntityOptions): InferableClass;
}
