import { TicketIcon as TicketIconBase } from '@starlightpro/shared-components';
import styled from 'styled-components';

export const TicketIcon = styled(TicketIconBase)`
  width: 24px;
  height: 25px;
`;
