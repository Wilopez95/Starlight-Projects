export enum ErrorCodes {
  ToLarge = 'file-too-large',
  InvalidType = 'file-invalid-type',
}

export enum MessageKeys {
  ToLarge = 'FILE_TOO_LARGE',
  InvalidType = 'INVALID_MIME_TYPE',
  Unknown = 'UNKNOWN',
}
