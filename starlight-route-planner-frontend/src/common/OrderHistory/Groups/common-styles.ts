import { Badge as BaseBadge } from '@starlightpro/shared-components';
import styled from 'styled-components';

export const Badge = styled(BaseBadge)`
  white-space: normal !important;
  word-break: break-all !important;
`;
