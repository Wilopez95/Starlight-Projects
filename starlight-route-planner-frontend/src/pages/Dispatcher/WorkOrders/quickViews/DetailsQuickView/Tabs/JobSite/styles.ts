import { MarkerIcon } from '@starlightpro/shared-components';
import styled from 'styled-components';

export const NoteWrapper = styled.div`
  background-color: var(--primary-gray-desaturated);
`;

export const StyledMarker = styled(MarkerIcon)`
  cursor: pointer;
  display: block;
  width: 37px;
  height: 52px;
`;
