/* eslint-disable no-unused-vars */
const JobSiteRepo = {
  getInstance() {
    return {
      getDefaultTaxDistricts({ activeOnly, jobSiteId }) {
        return [];
      },
    };
  },
};

export default JobSiteRepo;
