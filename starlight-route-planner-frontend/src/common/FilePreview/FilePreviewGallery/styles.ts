import styled from 'styled-components';

export const CountBlock = styled.div`
  background: var(--caption-desaturated);
  border-radius: 8px;
  font-style: normal;
  font-weight: normal;
  font-size: 12px;
  line-height: 14px;
  color: var(--white);
  padding: 1px 6px;
  text-align: center;
`;
