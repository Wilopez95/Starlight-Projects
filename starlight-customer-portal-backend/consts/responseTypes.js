// responseTypes for axios api
export const RESPONSE_TYPES = {
  arrayBuffer: 'arraybuffer',
  blob: 'blob',
  document: 'document',
  json: 'json',
  text: 'text',
  stream: 'stream',
};

export const RESPONSE_TYPES_VALUES = Object.values(RESPONSE_TYPES);
