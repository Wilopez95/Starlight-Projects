import { MultiSelect, Select } from '@starlightpro/shared-components';
import styled from 'styled-components';

export const StyledMultiSelect = styled(MultiSelect)`
  width: 25rem;
  align-self: center;
`;

export const StyledSelect = styled(Select)`
  width: 30rem;
  align-self: center;
`;
