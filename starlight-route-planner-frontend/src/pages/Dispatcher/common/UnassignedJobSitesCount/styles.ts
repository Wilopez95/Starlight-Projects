import styled from 'styled-components';

export const PreviewLabel = styled.div`
  position: absolute;
  left: 50%;
  transform: translateX(-50%) translateY(-50%);
  top: 32px;
  margin: 0 auto;
  padding: 7px 21px;
  background: #2b3949;
  box-shadow: 0px 8px 16px rgba(33, 43, 54, 0.1);
  border-radius: 29px;
`;
