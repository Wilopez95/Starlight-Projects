import { Modal as BaseModal } from '@starlightpro/shared-components';
import styled from 'styled-components';

export const Modal = styled(BaseModal)`
  top: 50% !important;
  width: 510px;
  transform: translate(-50%, -50%) !important;
`;
