import React from 'react';
import { Layouts } from '@starlightpro/shared-components';

export const HistoryRow: React.FC = ({ children }) => (
  <Layouts.Margin top="0.5" bottom="0.5">
    {children}
  </Layouts.Margin>
);
