import styled from 'styled-components';

export const Subitem = styled.div`
  margin-top: 8px;
  display: flex;
  align-items: center;

  svg {
    min-width: 20px;
    margin-left: 12px;
    transform: scale(0.9);
  }
`;
