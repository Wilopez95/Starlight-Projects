export const ORDER_REQUEST_STATUS = {
  requested: 'requested',
  confirmed: 'confirmed',
  history: 'history', // invoiced
  rejected: 'rejected',
};

export const ORDER_REQUEST_STATUSES = Object.values(ORDER_REQUEST_STATUS);
