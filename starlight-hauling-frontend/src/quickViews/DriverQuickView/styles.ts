import { Badge, FileUpload } from '@starlightpro/shared-components';
import styled from 'styled-components';

export const StyledFileUpload = styled(FileUpload)`
  width: 100%;
  height: 140px;
`;
export const StyledBadge = styled(Badge)`
  white-space: initial;
`;
