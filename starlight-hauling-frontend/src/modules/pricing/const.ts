export const enum RatesEntityType {
  oneTimeService = 'oneTimeService',
  recurringService = 'recurringService',
  oneTimeLineItem = 'oneTimeLineItem',
  recurringLineItem = 'recurringLineItem',
  threshold = 'threshold',
  surcharge = 'surcharge',
}
