import { Layouts } from '@starlightpro/shared-components';
import styled from 'styled-components';

export const RollOffContentWrapper = styled(Layouts.Box)`
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 16rem 3rem 3rem 3rem;
`;
