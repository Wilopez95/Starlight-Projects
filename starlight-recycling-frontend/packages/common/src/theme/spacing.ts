import createSpacing, { SpacingOptions } from '@material-ui/core/styles/createSpacing';

const spacingInput: SpacingOptions = 8;

export const spacing = createSpacing(spacingInput);

export default spacingInput;
