import styled from 'styled-components';

import { TablePageContainer, TableTools } from '@root/common/TableTools';

export const RecyclingPageContainer = styled(TablePageContainer)`
  padding: 0;
`;

export const RecyclingScrollContainer = styled(TableTools.ScrollContainer)`
  background: none;
`;
