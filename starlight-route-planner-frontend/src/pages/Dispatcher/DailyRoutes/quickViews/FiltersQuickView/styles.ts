import { UnassignedIcon as UnassignedIconBase } from '@starlightpro/shared-components';
import styled from 'styled-components';

export const UnassignedIcon = styled(UnassignedIconBase)`
  margin-right: 5px;
  height: 24px;
`;
