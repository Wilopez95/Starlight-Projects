import { InfoIcon as InfoIconBase } from '@starlightpro/shared-components';
import styled from 'styled-components';

export const InfoIcon = styled(InfoIconBase)`
  color: #e27900;
  transform: rotate(180deg);
`;
