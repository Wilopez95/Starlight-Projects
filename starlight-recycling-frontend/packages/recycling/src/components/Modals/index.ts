import { OpenModal as OpenModalType } from '@starlightpro/common/components/Modals';

export { default, openModal, closeModal } from '@starlightpro/common/components/Modals';

export type OpenModal = OpenModalType;
