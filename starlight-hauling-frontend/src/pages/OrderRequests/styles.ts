import styled from 'styled-components';

import { TablePageContainer } from '@root/common/TableTools';

export const OrderRequestsPageContainer = styled(TablePageContainer)`
  --tableHeaderHeight: 60px;
  font-size: 1.75rem;
`;
