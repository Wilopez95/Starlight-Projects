import { BackIcon as BackIconBase } from '@starlightpro/shared-components';
import styled from 'styled-components';

export const BackIcon = styled(BackIconBase)`
  margin-right: 11px;

  path {
    fill: #006fbb;
  }
`;

export const EditedLabel = styled.span`
  font-size: 14px;
  line-height: 20px;
  color: #919eab;
`;
