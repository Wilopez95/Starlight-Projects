import styled from 'styled-components';

import { TableCell } from '@root/common/TableTools';

export const TicketTableCell = styled(TableCell)`
  margin-top: 6px;
  margin-bottom: 6px;
  height: 24px;

  svg {
    height: 24px;
    width: 25px;
    margin-right: 1rem;
  }
`;
