import { Layouts } from '@starlightpro/shared-components';
import styled from 'styled-components';

export const FileInputLabel = styled(Layouts.Flex)`
  & input[type='file'] {
    display: none;
  }
`;
